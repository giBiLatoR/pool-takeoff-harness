# Local OpenAI-compatible API endpoint
API_BASE_URL=http://192.168.1.127:8080

# Model to use for VL extraction (change based on what's running on your local API)
VL_MODEL=qwen2.5-vl-72b-instruct  # or gpt-4o, claude, etc.

# Optional: port override (default 3000)
PORT=3000
